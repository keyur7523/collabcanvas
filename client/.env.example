# API URL - WebSocket URL is derived from this automatically
# Protocol is auto-detected: https → wss, http → ws
VITE_API_URL=http://localhost:8000

# Optional: Override WebSocket URL (only if different from API URL)
# VITE_WS_URL=ws://localhost:8000/ws
